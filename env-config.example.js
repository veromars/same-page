// Copy this file to env-config.js and fill in your real Supabase project
// values. env-config.js is gitignored — never commit real keys.
window.__ENV__ = {
  SUPABASE_URL: '',
  SUPABASE_ANON_KEY: '',
};
